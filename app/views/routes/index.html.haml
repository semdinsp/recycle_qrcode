%h1 Routes#index
%p Find me in app/views/routes/index.html.haml
%table(class="table table-hover table-condensed")
  %thead
    %tr
      %th Route
      %th Name
      %th
      %th

  %tbody
    - @myroutes.each do |myroute|
      %tr
        %td= myroute.id
        %td= myroute.name
        %td= link_to 'Show', "/routes/show/#{myroute.id}"

%br
