- content_for :head do
  :javascript
    function binURL() { return "/bins.json?icon=normal"; };
    function mapCenter() { return {
      lat: #{SETTINGS.centerlat},
      lng: #{SETTINGS.centerlong}   } };
    //console.log("setting binURL to: ", binURL, "map center", mapCenter())


%h1 Map of Bin Locations
%p  This is the map of our bin locations and also a reporting system.
You can see the truck routes and when bins have been collected as well.
#map{:style => "height: 400px; width: 400;"}
%h3 Legend
%dl
  %dt.attribute-label Red Icon
  %dd.attribute-data Activity Older than one day
  %dt.attribute-label Green Icon
  %dd.attribute-data Recent activity withn 24 hours


= render 'links', locals: { route: true, collection: true, bin: false}
